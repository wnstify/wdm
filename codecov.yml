coverage:
  precision: 1
  round: down
  range: "80...100"
  status:
    project:
      default:
        target: auto
        threshold: 0.5%
        base: auto
    patch:
      default:
        target: 70%
        threshold: 5%

comment: false

github_checks:
  annotations: false
